#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <stdarg.h>
#include <stdlib.h>
#include <unistd.h>
int putchar(int c);
int _printf(const char *format, ...);
int printf1_printf(const char *format, ...);
#endif
